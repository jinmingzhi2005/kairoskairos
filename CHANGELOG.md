# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## 1.2.0

### 农历与节气
- 日历格子上显示农历日期（初一、廿七），传统节日（春节 / 除夕 / 元宵 / 端午 / 中秋 / 重阳 / 腊八 …）与 24 节气会自动替换显示
- 配色区分：法定假期与农历传统节日为红、节气为绿、一般公历节日为紫、平常农历日为灰
- 农历**不自己算**，直接用运行时内置的 ICU 中文历法（`Intl.DateTimeFormat('zh-CN-u-ca-chinese')`）—— 零依赖、零数据表，也不用维护 1900–2100 的农历数据表
- 节气自己算：用太阳视黄经判定，与公开天文年历逐条核对过
- 设置里可关闭

### 深色模式
- 跟随系统切换；设置里可强制浅色 / 深色
- 实现上主进程设 `nativeTheme.themeSource`，渲染层只用一条 `prefers-color-scheme` 媒体查询 —— 不需要额外的 IPC 往返
- 启动时在窗口创建**之前**定好主题，避免闪一下浅色

### 工程
- `test:unit` 从 30 项扩到 40 项：新增农历日期、闰月、除夕判定、节气日期抽样、全年节气数量校验
- 冒烟测试支持 `KAIROS_DARK=1` 在深色下截图，便于人工核对
- 修掉日期格被内容撑高的问题（`aspect-ratio` 管不住 flex 的 `min-height: auto`）

## 1.1.0

### 日历窗改为「贴着桌面」
- 默认**不再浮在所有窗口最上方**，而是贴在桌面：切到别的程序时会被盖住，只在你看桌面的时候出现
- 实现上关闭窗口置顶，并在**失焦后**通过系统接口把它压到 Z 序底部；接口调用失败会静默降级，不影响使用
- 用托盘图标或 `Alt+Space` 唤出日历时会先把它抬到可见位置，失焦后再自动沉回去
- 设置面板里可随时切回「始终置顶」
- **强提醒不受这个设置影响**，任何时候都保证在最上方

### 强提醒置顶加固
- 层级提升到 `screen-saver` + `relativeLevel 1`
- 弹出后 30 秒内每 1.5 秒重申一次置顶，覆盖全屏播放器、部分游戏、以及安全软件自己弹的置顶窗口
- 显示时补一次 `moveTop()` —— `focus()` 只保证拿到焦点，不保证抬到最上面

### 工程
- 新增 `npm run test:window`：10 项窗口层级断言（日历不置顶 / 切换生效 / 提醒必置顶）
- `clean:artifacts` 的降级策略由「清空内容」改为「改名隔离 + 打包排除」：
  0 字节文件照样会被打进包里，清空等于没清

## 1.0.0

首个正式版本。此前的开发代号是 DeskBox，1.0 起更名为 **Kairos**。

### 日历与待办
- 常驻桌面的无边框玻璃拟态日历卡片，支持拖动与 8 方向缩放，位置尺寸自动记忆
- 待办支持重复规则（不重复 / 每天 / 工作日 / 每周 / 每月）与提前提醒量（准时 / 5 / 15 / 30 分钟）
- 日历格子用彩色圆点区分提醒级别（红=强提醒、蓝=系统通知、灰=不提醒）

### 提醒
- **每条待办两个独立的提醒开关**，可任意组合，包含「先弹系统通知、5 分钟后仍未处理再升级为强提醒」的两段式提醒
- 待办列表里直接点标签就能改提醒方式，无需删除重建
- 强提醒走独立弹窗 + 合成提示音；系统通知走 Windows 原生通知
- 支持「稍后提醒」与「本次已完成」（重复事项下会记入本轮，下个周期继续）

### 法定节假日
- 内置国务院办公厅发布的放假安排，放假标红、调休补班的周末标「班」
- 覆盖 2025 / 2026 年；无数据的年份自动降级为「只标公历节日」

### 电脑日历同步
- Outlook 桌面版直连（走 COM，无需导出）
- ICS 文件导入与 ICS / webcal 订阅，支持多源并存、独立开关与配色
- 同步失败只记录原因并保留上次数据，不会清空日历

### 系统集成
- 托盘菜单：显示日历 / 开机自启 / 测试强提醒 / 摆回右上角 / 退出
- 全局热键 `Alt+Space` 显示日历、`Ctrl+Alt+C` 切换显示
- 开机自启默认开启，支持显示器拔插后自动把窗口拉回可见区域

### 工程
- 65 项自动化测试：纯逻辑单元测试、提醒调度、托盘数据通道、日历同步全链路、渲染冒烟
- 从旧版 DeskBox 平滑迁移用户数据（只搬运 json 配置，不碰缓存）
