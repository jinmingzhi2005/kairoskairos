# 统一换行符，避免 Windows / macOS / Linux 之间产生无意义的整文件 diff
* text=auto eol=lf

# 二进制资源不做任何转换
*.png binary
*.jpg binary
*.ico binary
*.exe binary
*.dll binary
*.asar binary

# 这些文件必须保持原样
package-lock.json -diff linguist-generated
